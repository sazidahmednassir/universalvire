{
  "name": "customvire",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "visual": "node utils/screenshotFromExcel.js",
    "clean:allure": "rm -rf allure-results allure-report",
    "regression": "npm run clean:allure && playwright test tests/regression && allure serve allure-results",
    "allure:report": "allure generate allure-results -o allure-report --clean && allure open allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "playwright": "^1.55.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "allure-commandline": "^2.34.1",
    "allure-playwright": "^2.15.1"
  }
}
